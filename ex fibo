#include <stdio.h>

long long fibo(int n) {
    long long a = 0, b = 1, prox;
    for (int i = 2; i <= n; i++) {
        prox = a + b;
        a = b;
        b = prox;
    }
    return b;
}

long long fiborec(int n) {
    if (n <= 1) return n;
    return fiborec(n - 1) + fiborec(n - 2);
}
